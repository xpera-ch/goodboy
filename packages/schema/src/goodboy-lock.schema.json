{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://goodboyjs.com/schemas/goodboy-lock/v1",
  "title": "GoodBoy Lock",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "generated", "skills"],
  "properties": {
    "schema": {
      "type": "string",
      "pattern": "^1\\.\\d+\\.\\d+$",
      "maxLength": 32,
      "description": "GoodBoy goodboy.lock schema version, semver-shaped (v1.x only). CLIs tolerate a newer minor than they know by ignoring unknown top-level fields (with a warning); a field added inside a skill entry fails validation and the lock is treated as absent, then regenerated on the next install or upgrade. A newer major is rejected."
    },
    "generated": {
      "type": "string",
      "format": "date-time",
      "maxLength": 32,
      "description": "When the lock was last written, ISO-8601. Machine-generated; refreshed on every write."
    },
    "skills": {
      "type": "object",
      "description": "Resolved install state as a name-to-entry map. Names must match the skill-name pattern.",
      "propertyNames": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$",
        "maxLength": 64
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["version"],
        "properties": {
          "version": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "maxLength": 32,
            "description": "Exact installed version, semver-shaped."
          },
          "integrity": {
            "type": "string",
            "maxLength": 256,
            "description": "Content-integrity hash computed at install/upgrade time. The hash construction itself is owned by integrity.ts and treated as versioned/frozen — no pattern is enforced here."
          }
        }
      }
    }
  }
}
