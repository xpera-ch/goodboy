{
  "name": "goodboy",
  "version": "0.1.0",
  "private": true,
  "description": "A personal skill registry and package manager for Claude Code and the Agent Skills ecosystem",
  "workspaces": [
    "packages/cli",
    "packages/schema",
    "packages/registry-client"
  ],
  "scripts": {
    "generate:types": "npm run generate -w packages/schema",
    "verify:types": "npm run generate:types && git add -N -- packages/schema/generated/ && git diff --exit-code packages/schema/generated/",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "publish:schema": "npm publish --workspace packages/schema --access public",
    "publish:cli": "npm publish --workspace packages/cli --access public",
    "publish:all": "npm run publish:schema && npm run publish:cli"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "json-schema-to-typescript": "^15.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
