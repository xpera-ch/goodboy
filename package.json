{
  "name": "goodboy",
  "version": "0.0.1",
  "private": true,
  "description": "A personal skill registry and intelligent dispatcher for Claude Code",
  "workspaces": [
    "packages/cli",
    "packages/schema",
    "packages/registry-client"
  ],
  "scripts": {
    "generate:types": "npm run generate -w packages/schema",
    "verify:types": "npm run generate:types && git diff --exit-code packages/schema/generated/"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "json-schema-to-typescript": "^15.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
